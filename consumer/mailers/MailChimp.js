var MailChimp;
(function () {

	MailChimp = {};
	_.extend(MailChimp, Events);
	_.extend(MailChimp, {
		registerUser: function (fbProfile) {
			// This won't do anything yet. I need to build 
			// some infrastructure on the server side to support this
		}
	});

})();